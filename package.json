{
  "name": "turkish-trading-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "server": "dotnet run --project backend",
    "db:migrate": "dotnet ef database update --project backend",
    "seed": "echo Seeding runs automatically on server startup",
    "install:all": "npm install --prefix frontend"
  }
}
